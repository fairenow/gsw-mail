DATABASE_URL=postgres://gsw_mail:REPLACE_PASSWORD@postgres:5432/gsw_mail
STALWART_JMAP_URL=https://mx1.guidedstepswellness.com
STALWART_ADMIN_TOKEN=replace-me
# Basic credential for token introspection and explicit background recovery jobs.
# Normal request mailbox operations use the caller's OAuth bearer token.
STALWART_MAIL_USERNAME=ramon@team.guidedstepswellness.com
STALWART_MAIL_PASSWORD=replace-me
OIDC_ISSUER=https://mx1.guidedstepswellness.com
OIDC_CLIENT_ID=gsw-mail-web
# Introspection authenticates as the mailbox service account above (account login, not an OAuth client)
IDENTITY_PROVIDER=stalwart
RESEND_API_KEY=replace-me
DELIVERY_WEBHOOK_SECRET=replace-me
SEND_DELAY_SECONDS=5
DEV_USER_ID=
