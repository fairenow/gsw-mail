# Copy to .env.local (gitignored) and replace the value before running any Hetzner provisioning.
# Hetzner applications-scoped API token; the access token must be an API token, not a project password.
HETZNER_API_TOKEN=